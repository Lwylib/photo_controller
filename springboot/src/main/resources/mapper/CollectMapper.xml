<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.CollectMapper">

    <select id="selectAll" resultType="com.example.entity.Collect">
#         select collect.*, user.name as userName, category.name as categoryName from collect
#         left join user on collect.user_id = user.id
#         left join category on collect.category_id = category.id

        select collect.*, user.name as userName, category.name as categoryName from category
            left join user on category.user_id = user.id
                                  left join collect on collect.category_id = category.id
        <where>
            <if test="id != null"> and collect.id = #{id}</if>
            <if test="userId != null"> and collect.user_id = #{userId}</if>
            <if test="userName != null"> and user.name like concat('%', #{userName}, '%')</if>
            <if test="categoryId != null"> and collect.category_id = #{categoryId}</if>
            <if test="categoryName != null"> and category.name like concat('%', #{categoryName}, '%')</if>
        </where>
        order by id desc
    </select>

    <select id="selectById" resultType="com.example.entity.Collect">
        select * from `collect`
        where id = #{id}
    </select>

    <delete id="deleteById">
        delete from `collect`
        where id = #{id}
    </delete>

    <delete id="deleteByUserId">
        delete from `collect`
        where user_id = #{id}
    </delete>

    <insert id="insert" parameterType="com.example.entity.Collect" useGeneratedKeys="true" keyProperty="id">
        insert into `collect`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="userId != null">user_id,</if>
            <if test="categoryId != null">category_id,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="userId != null">#{userId},</if>
            <if test="categoryId != null">#{categoryId},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.example.entity.Collect">
        update `collect`
        <set>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
        </set>
        where id = #{id} 
    </update>

</mapper>